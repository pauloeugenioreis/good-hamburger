global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'GoodHamburger-monitor'

scrape_configs:
  # GoodHamburger API
  - job_name: 'GoodHamburger-api'
    scrape_interval: 10s
    static_configs:
      - targets: ['api:80']
        labels:
          service: 'api'
          environment: 'development'

  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
